services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: idemledger
      POSTGRES_USER: idem
      POSTGRES_PASSWORD: idem
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
